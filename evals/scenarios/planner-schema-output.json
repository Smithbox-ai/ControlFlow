{
  "id": "planner-schema-output",
  "target_agent": "Planner",
  "goal": "Planner creates a markdown plan file conforming to schemas/planner.plan.schema.json and provides a concise handoff message",
  "input": {
    "query": "Plan a focused refactor of the authentication API error handling",
    "context": "The request is unambiguous and should produce a normal planning response without clarification."
  },
  "expected": {
    "schema": "schemas/planner.plan.schema.json",
    "plan_file_created": true,
    "executor_agent_required_in_all_phases": true,
    "phase_required_fields": [
      "executor_agent"
    ],
    "handoff_message_present": true,
    "risk_review_present": true,
    "risk_review_covers_all_categories": true,
    "must_not": "raw JSON output to chat"
  }
}
