# Codex Host Overrides

Codex is currently the canonical source for the declared shared skill and template targets, so no Codex overlay files are required.

Codex-only skill agent files such as `agents/openai.yaml` remain tracked standalone outputs under `plugins/controlflow-codex/skills/` and are intentionally unmanaged by the Phase 5 shared generator manifest.
