# Cursor Host Overrides

The portable plan, verify, and review skills are generated directly from shared-source.
Cursor package-specific agents and installation scripts remain outside the managed skill
target and must not reintroduce the retired ten-skill workflow.
